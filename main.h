#ifndef MAIN_H
#define MAIN_H

/* C standard library header file */
#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>

/* function prototype */
int _putchar(char c);
int _printf(const char *format, ...);
int _print_str(char *s);

#endif /* MAIN_H */
